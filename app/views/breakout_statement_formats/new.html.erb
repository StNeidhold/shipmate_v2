<!-- Validation failure messages -->
<% if @breakout_statement_format.errors.any? %>
  <% @breakout_statement_format.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Breakout Statement Format</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_breakout_statement_format" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for description -->
      <div class="form-group">
        <label for="description" class="control-label">
          Description
        </label>

        <input type="text" id="description" name="description" placeholder="Description" class="form-control" value="<%= @breakout_statement_format.description %>">
      </div>

      <button class="btn btn-success">
        Create Breakout Statement Format
      </button>
      or
      <a href="/breakout_statement_formats">Cancel</a>
    </form>
  </div>
</div>

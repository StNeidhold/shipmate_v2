<div class="page-header">
  <h1>
    Eval results
    <a href="/eval_results/new" class="btn btn-lg btn-success">New Eval result</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Results description</th>
        <th>Actions</th>
      </tr>

      <% @eval_results.each do |eval_result| %>
      <tr>
        <td><%= eval_result.results_description %></td>
        <td>
          <a href="/eval_results/<%= eval_result.id %>" class="btn btn-primary">Show</a>
          <a href="/eval_results/<%= eval_result.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_eval_result/<%= eval_result.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
